[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    data_handling: test belongs to data_handler functionality
    preprocess: test belongs to preprocess functionality
filterwarnings =
    ignore::DeprecationWarning
